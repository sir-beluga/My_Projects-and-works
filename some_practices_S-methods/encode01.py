name="kushal"
x=name.encode("utf-8")
# encode() basically yeets my string into byte-language so the computer can vibe with it
# even if it looks the same, it's NOT a normal string anymore (it's bytes, bro)
print(x)
print(type(x))